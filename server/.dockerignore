# Артефакти локальної збірки — не потрібні в контексті docker (їх зробить етап build).
bin/
obj/
# Локальні секрети/налаштування (у контейнер секрети йдуть через env-змінні).
*.user
appsettings.Development.json
# Завантажені фото — монтуються томом, не пекуться в образ.
wwwroot/uploads/
